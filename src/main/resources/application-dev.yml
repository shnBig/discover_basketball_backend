spring:
  datasource:
    # 本地SSH隧道端口3307，对应服务器3306
    url: jdbc:mysql://localhost:3307/discover_basketball?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=convertToNull&transformedBitIsBoolean=true&allowMultiQueries=true&tinyInt1isBit=false
    username: root
    password: "@Sunhaonan030323"  # 保留你原有的密码格式（双引号包裹）
    # 开发环境连接池配置（按需调整）
    hikari:
      maximum-pool-size: 10  # 最大连接数
      minimum-idle: 5        # 最小空闲连接数
      connection-timeout: 30000  # 连接超时时间（30秒）